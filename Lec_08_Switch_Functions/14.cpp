// void

/*
🔹 1. void as a Return Type
When a function does not return a value, its return type is void.

✅ Example:

   void sayHello()
   {
      cout << "Hello!" << endl;
   }

This function performs an action (prints "Hello!") but does not return any value.

🔹 2. void as a Parameter Type
A function that takes no arguments can use void to indicate this.

✅ Example:

   void greet(void)
   {
      cout << "Hi!" << endl;
   }

void in the parameter list means the function takes no parameters. In C++, you can also just write greet() — the (void) style is more common in C.

*/